<ContentDialog
    x:Class="MailApp.Views.Dialogs.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MailApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tkui="using:CommunityToolkit.WinUI.Controls"
    xmlns:m="using:MailApp.Models"
    mc:Ignorable="d"
    Title="Login"
    CloseButtonText="Cancel"
    x:Name="root">

    <Grid>
        <ItemsControl ItemsSource="{Binding ElementName=root,Path=MailAuthServices}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Spacing="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="m:MailAuthServiceWrapper">
                    <tkui:SettingsCard Header="{x:Bind Name}" 
                                       Command="{x:Bind LoginCommand}"
                                       CommandParameter="{Binding ElementName=root}"
                                       IsClickEnabled="True"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</ContentDialog>
